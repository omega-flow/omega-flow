TO FIX:

- primitives - each has his own style, we need to have some common style for all nodes
- NodeTypeDefintion - double information about view in ViewComponents as in NodeTypeDefinition itself
- Localization - support localization in editor components
- Do we needs to store name in workflow definition itself?
